namespace NIDE
{
    public enum ImageType
    {
        ITEMS_OPAQUE,
        TERRAIN_ATLAS
    }

    public enum ProjectType
    {
        MODPE,
        COREENGINE
    }

    public enum DialogType
    {
        SCRIPT,
        TEXTURE,
        LIBRARY
    }
}