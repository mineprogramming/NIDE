namespace NIDE
{
    public enum WorkMode
    {
        JAVASCRIPT,
        MODPKG,
        CORE_ENGINE
    }

    public enum ImageType
    {
        ITEMS_OPAQUE,
        TERRAIN_ATLAS
    }

    public enum ProjectType
    {
        MODPE,
        LIBRARY,
        COREENGINE
    }
}